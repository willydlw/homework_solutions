#include "EntityTest.h"
#include "testUtility.h"

const size_t MAX_SIZE_T = static_cast<size_t>(0 - 1);
/*
int main()
{
	{
		EntityTest et;
		et.testDefaultConstructor();
	}

	{
		int tagLength = random_integer(0, 16);
		std::string randomTag = random_string(tagLength);
		size_t id = random_sizet(0, MAX_SIZE_T);
		EntityTest et;
		et.testConstructor(randomTag, id);
	}


	return 0;
}

*/